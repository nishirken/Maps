import styled from 'styled-components';

const StyledMarkerName = styled.h5`
    max-width: ${StyleFunc.em(140)};
    font-size: ${StyleFunc.em(18)};
    word-wrap: break-word;
`;

export default StyledMarkerName;
